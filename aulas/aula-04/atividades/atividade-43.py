# Pergunte ao usuário quantas vezes ele quer que uma mensagem seja exibida. 
mensagem = str(input('Digite aqui a mensagem que quer ver repetida: \n'))

# Use um for para imprimir essa mensagem repetidas vezes.
for i in range(10):
    print(f'Sua mansagem é:{mensagem}')
    
